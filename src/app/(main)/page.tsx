import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="p-40 flex items-center place-content-center">
      <button className="p-4 bg-blue-500 text-white rounded-md">
        make pdf
      </button>
    </div>
  );
};

export default page;
